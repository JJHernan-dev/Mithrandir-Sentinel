<Application x:Class="Mithrandir_Sentinel.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Mithrandir_Sentinel"
             xmlns:vm="clr-namespace:Mithrandir_Sentinel.ViewModels"
             xmlns:views="clr-namespace:Mithrandir_Sentinel.Views"
             StartupUri="MainWindow.xaml">

    <Application.Resources>

        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="Resources/Styles/DataGridStyles.xaml"/>
                <ResourceDictionary Source="Resources/Styles/SidebarStyles.xaml"/>

            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                <views:DashboardView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:ConnectionsViewModel}">
                <views:ConnectionsView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:AlertsViewModel}">
                <views:AlertsView/>
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>

        </ResourceDictionary>

    </Application.Resources>

</Application>